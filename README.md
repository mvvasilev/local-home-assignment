# Local Home Assignment

![img.png](img.png)

## Build

This repository contains dockerfiles and a docker-compose file.
To build and run everything, use `./gradlew clean build && docker-compose up --build`